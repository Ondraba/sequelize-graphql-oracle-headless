import { createComponent } from 'react-fela'

const ContainerFluid = createComponent(() => ({
  maxWidth: '100%',
  lineHeight: 1.5,
  paddingLeft: 15,
  paddingRight: 15
}))

export default ContainerFluid
